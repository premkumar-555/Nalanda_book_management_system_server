const avoidTimeSeries = { createdAt: 0, updatedAt: 0 };

const BORROWED = "BORROWED";
const RETURNED = "RETURNED";
const ADMIN = "ADMIN";
const MEMBER = "MEMBER";

module.exports = {
  BORROWED,
  RETURNED,
  ADMIN,
  MEMBER,
};
